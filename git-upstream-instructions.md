Pulling upstream changes into your fork

   - git fetch upstream
   - git rebase upstream/main. 
   - Resolve any conflicts
   - run checks
   - git push origin main --force-with-lease to update your fork (or use VS Code’s Force Push).

   